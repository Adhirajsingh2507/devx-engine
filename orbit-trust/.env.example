# ORBIT-TRUST environment (doc 14). Placeholders only. Real values live in the
# deployment service's secret settings and separate local/preview/production
# scopes. Never commit real values; never print them in logs/traces/reports.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Groq (server secret)
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b
ADK_LITELLM_MODEL=groq/openai/gpt-oss-20b
GROQ_ENABLED=false

# App
APP_PUBLIC_ORIGIN=
APP_MODE=public_synthetic
POLICY_VERSION=demo-2.0
APP_REQUEST_BUDGET_SECONDS=45
ORBIT_CORE_THREADS=1
