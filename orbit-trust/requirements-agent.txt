# Local/dev deps for the N3 live ADK↔Groq agent (doc 09). NOT deployed to Vercel
# (too heavy for a serverless function; imported lazily so prod runs without them).
# Tested on Python 3.12. litellm 1.100.1 is past the compromised 1.82.7/1.82.8.
google-adk==2.9.0
litellm==1.100.1
groq==1.7.0
